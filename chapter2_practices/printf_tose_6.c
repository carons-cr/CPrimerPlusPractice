#include <stdio.h>

int main(void)
{
    int tose = 10;
    int tose_twice = tose*2;
    int tose_square = tose*tose;
    printf("tose is %d, it's twice = %d, it's square = %d.\n", tose, tose_twice, tose_square);
    return 0;
}
